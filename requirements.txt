watchdog>=4.0.0
Pillow>=10.0.0
zwoasi>=0.2.0
numpy>=2.5.3
opencv-python>=5.0.0.93
requests>=2.31.0
platformdirs>=4.0.0  # Cross-platform app-data root (services/utils_paths.py)
pystray>=0.19.5
astral>=3.2  # Accurate sunrise/sunset/twilight calculations
scipy>=1.10.0  # Fisheye lens calibration (scipy.optimize.least_squares)
astropy>=5.0.0  # FITS file support for ML data contribution

# PySide6 Fluent UI (new modern interface)
# Pinned to the version the installer ships. CI installs from this file, so an
# unpinned range tested 6.11 while users ran 6.10 — and 6.11 changed QLocalSocket
# behaviour on Windows (single-instance guard). Bump deliberately, with a test run.
PySide6==6.10.2
PySide6-Fluent-Widgets[full]>=1.5.0
QtAwesome>=1.3.0  # Material Design Icons (nav rail, all-sky settings)

# Analytics + observability (PostHog events + log forwarding)
posthog>=7.0.0
opentelemetry-sdk>=1.20.0          # OTLP log pipeline (services/posthog_logs.py)
opentelemetry-exporter-otlp-proto-http>=1.20.0  # ships app logs to PostHog

# ML inference (lightweight ONNX runtime for production)
onnxruntime>=1.16.0

# YouTube timelapse uploads
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
uritemplate>=4.1.0
